import type { ScheduleSection } from '../../types';
type Props = {
    section: ScheduleSection;
    data?: any;
};
export default function Schedule({ section, data }: Props): import("react/jsx-runtime").JSX.Element;
export {};
