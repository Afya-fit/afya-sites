import type { HeroSection } from '../../types';
type HeroProps = {
    section: HeroSection;
};
export default function Hero({ section }: HeroProps): import("react/jsx-runtime").JSX.Element;
export {};
